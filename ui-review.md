# PULSE·16 UI/UX 리뷰 및 개선

리뷰 관점: **그루브박스·스텝 시퀀서 사용자**가 실제로 비트를 찍는 흐름
(프리셋 고르기 → 패턴 편집 → 음색 조정 → 반복 → 저장)에서 무엇이 막히는가.

기준으로 삼은 것: Elektron Digitakt / Roland TR-8S / Ableton Drum Rack /
Teenage Engineering PO 시리즈의 조작 관습.

---

## 1. 심각도별 문제 목록

### 🔴 치명 — 작업 결과를 잃거나 되돌릴 수 없음

| # | 문제 | 근거 |
|---|---|---|
| 1 | **실행취소가 없다** | `Clear`·`Seed` 버튼이 패턴을 즉시 파괴. 되돌릴 방법 없음. 프리셋 칩을 잘못 누르면 편집분이 전부 날아감 |
| 2 | **새로고침하면 전부 사라진다** | 뱅크 A~D는 메모리에만 존재. 30분 작업한 패턴이 탭을 닫으면 소멸 |

### 🟠 중대 — 도구의 핵심 작업이 느리거나 어긋남

| # | 문제 | 근거 |
|---|---|---|
| 3 | **정보 위계가 역전** | 화면 세로 절반을 노브 30개(Tune/Bass/Master/Studio 4줄)가 차지하고, 정작 비트를 찍는 시퀀서는 스크롤해야 나옴. 하드웨어 그루브박스는 예외 없이 그리드가 최상단·최대 면적 |
| 4 | **드래그로 스텝을 칠할 수 없다** | 16분 하이햇을 채우려면 16번 클릭. 모든 스텝 시퀀서가 지원하는 기본 동작 |
| 5 | **키보드 조작이 스페이스바뿐** | 뱅크 전환·실행취소 등 반복 동작이 전부 마우스 |
| 6 | **재생 위치 표시가 뭉개짐** | `.pad`에 `transition:background .08s`가 걸려 있어 174 BPM(16분=86ms)에서 잔상이 남음 |

### 🟡 보통 — 오해·오조작을 유발

| # | 문제 | 근거 |
|---|---|---|
| 7 | **Swing 노브 단위 혼동** | `%`로 표시됐으나 실제 스윙 비율(50~66%)과 다른 단위. 노브 38 = 스윙 62.5% |
| 8 | **클리핑 경고가 없다** | Out 미터에 0dB 마커(`88%` 위치)는 있으나 넘어가도 시각 경고 없음 |
| 9 | **뮤트 버튼이 9px** | 권장 터치 타겟(44px)의 1/5. 옆의 볼륨 슬라이더를 잘못 잡기 쉬움 |
| 10 | **벨로시티 3단계 구분이 약함** | `data-v=1`과 `2`가 명도 차 + 상단 흰 줄뿐. 어두운 트랙 색에서는 거의 안 보임 |
| 11 | **라벨 한/영 혼용** | "Kit 일괄 적용"·"전체 받기" 옆에 "Sample Trim"·"Loop Vol" |

### 🔵 접근성

| # | 문제 | 근거 |
|---|---|---|
| 12 | **패드에 접근성 정보 없음** | `div` + `tabIndex`만 있고 `role`·`aria-label` 없음. 스크린리더로 "몇 번 트랙 몇 번째 스텝인지" 알 수 없음 |
| 13 | **상태를 색으로만 전달** | 트랙 구분이 색상 단독. 색각 이상 사용자는 구분 불가 |
| 14 | **모바일 사실상 불가** | 34px 패드 + 가로 스크롤. 별도 설계 필요 |

---

## 2. 이번에 반영한 개선

### 🔴 1. 실행취소 (Ctrl+Z / Ctrl+Shift+Z)
패턴을 바꾸는 모든 동작 직전에 스냅샷을 쌓습니다(최대 60단계).
대상: 패드·노트 편집, 프리셋 칩, 트랙별 출처 변경, Seed, Clear, 뱅크 전환.
되돌리기·다시하기 양방향을 지원하고, 상태 표시줄에 남은 단계를 보여줍니다.

### 🔴 2. 자동 저장 · 복원
`localStorage`에 **뱅크 4개 + 모든 노브·셀렉트 값 + 트랙 상태**를 저장합니다.
- 변경 후 400ms 디바운스로 저장 (연타해도 부하 없음)
- 새로고침하면 마지막 상태를 그대로 복원
- `저장 초기화` 버튼으로 공장 초기화 가능
- 샘플 배정은 네트워크 의존이라 저장 대상에서 제외 (합성으로 복원)

### 🟠 3. FX 랙 접기 — 시퀀서를 화면 위로
`Tune / Bass / Master / Studio` 4개 패널을 **개별 토글**로 바꾸고
**기본을 접힘**으로 두었습니다. 접힘 상태도 저장됩니다.
결과: 첫 화면에서 시퀀서 그리드가 바로 보입니다(세로 약 300px 절약).

### 🟠 4. 드래그 페인트
패드를 누른 채 끌면 같은 값으로 연속 입력됩니다.
- 빈 칸에서 시작 → 끌면서 **보통 강도로 채움**
- 채워진 칸에서 시작 → 끌면서 **지움**
- 베이스 피아노롤도 동일하게 동작
16분 하이햇을 한 번의 드래그로 채울 수 있습니다.

### 🟠 5. 키보드 단축키
| 키 | 동작 |
|---|---|
| `Space` | 재생 / 정지 |
| `1` `2` `3` `4` | 뱅크 A~D 전환 |
| `Ctrl+Z` | 실행취소 |
| `Ctrl+Shift+Z` | 다시하기 |
| `?` | 단축키 도움말 |
| `Esc` | 도움말 닫기 |

### 🟠 6. 재생 위치 표시 개선
`.pad.hit`·`.note.hit`에서 트랜지션을 제거해 즉시 반응하도록 했습니다.
빠른 템포에서 잔상이 사라집니다.

### 🟡 7. Swing 단위 명확화
읽기 값을 `38 노브 · 63% 스윙` 형태로 바꿔 **파생된 실제 스윙 비율**을
함께 보여줍니다. 노브 50부터 표시가 `67% 셔플`로 바뀌어 완전 셔플 지점을
눈으로 확인할 수 있습니다.

환산식은 코드의 실제 계산에서 유도했습니다 —
`shift = 16분 × (노브/100) × 0.66` 이므로 8분쌍 안에서의 위치는
`(1 + 0.66×노브/100) / 2` = **50 + 0.33 × 노브** (%).

### 🟡 8. 클리핑 경고
Out 미터가 −0.3dB를 넘으면 수치가 빨갛게 점등하고 `CLIP` 표시가 뜹니다.
2초간 유지되어 순간 피크도 놓치지 않습니다.

### 🟡 9. 뮤트 버튼 타겟 확대
점의 겉모습은 그대로 두고, 보이지 않는 클릭 영역을 상하좌우 10px씩
넓혔습니다(실질 29px). 옆 슬라이더 오조작이 줄어듭니다.

### 🟡 10. 벨로시티 구분 강화
강세(`data-v=2`)의 상단 표시줄을 더 두껍고 선명하게,
보통(`data-v=1`)에는 하단에 얇은 밑줄을 추가해
**흑백으로 봐도 세 단계가 구분**되도록 했습니다.

### 🔵 12. 패드 접근성
모든 패드·노트에 `role="button"`과
`aria-label="Kick 3박 두 번째 스텝"` 형식의 레이블,
현재 상태를 나타내는 `aria-pressed`를 부여했습니다.

---

## 3. 반영하지 않은 것과 이유

| # | 항목 | 이유 |
|---|---|---|
| 11 | 라벨 한/영 통일 | 어느 쪽으로 통일할지가 취향 문제라 임의로 정하지 않았습니다. 지정해주시면 일괄 적용하겠습니다 |
| 13 | 색각 대응 | 트랙마다 색 + **패턴(사선·점)** 을 겹치는 방식이 정석인데, 패드가 34px이라 문양이 뭉갭니다. 패드 크기 조절 기능과 함께 다루는 편이 낫습니다 |
| 14 | 모바일 대응 | 세로 배치·스텝 페이지네이션(1~8 / 9~16) 등 별도 레이아웃 설계가 필요해 범위가 큽니다 |

---

## 4. 다음 후보

1. **패턴 길이 가변** (1~16스텝) — 폴리미터 실험이 가능해짐
2. **스텝별 ratchet · 마이크로 타이밍** — 장르 커버리지가 크게 늘어남
   ([patterns/README.md](patterns/README.md) §3)
3. **A/B 패턴 체인** — 뱅크를 순차 재생해 8마디 구성 만들기
4. **패턴 내보내기** — MIDI 또는 JSON으로 저장/공유
5. **파형·엔벨로프 미리보기** — 킥 음색 조정 시 눈으로 확인
