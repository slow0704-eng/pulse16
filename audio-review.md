# 사운드 품질 검사

두 갈래로 진행했습니다.

1. **실측** — `OfflineAudioContext` 로 각 악기를 단독 렌더링해 피크·RMS·DC·대역 분포를 측정
2. **코드 감사** — 구현 정확성(엔지니어 관점)과 음색 설계(사운드 디자인 관점) 검토

> 측정은 48 kHz 모노, 벨로시티 1.0, 트랙 게인·마스터 체인을 거치지 **않은** 소스 단독 값입니다.
> 재현 파일: `tools/measure-voices.html`, `tools/measure-master.html`

---

## 1. 실측 결과 — 악기 단독

| 악기 | 피크 dBFS | RMS dBFS | DC 오프셋 | 스펙트럼 중심 |
|---|---|---|---|---|
| kick / deep | **+2.8** | −17.5 | 4.9e−5 | 89 Hz |
| kick / punch | **+3.7** | −17.5 | 1.1e−4 | 132 Hz |
| kick / tight | **+3.6** | −18.0 | −2.1e−5 | 160 Hz |
| kick / wood | **+3.6** | −17.7 | 3.0e−5 | 113 Hz |
| snare / body | **+3.6** | −21.7 | −3.4e−4 | 1602 Hz |
| snare / crack | **+4.8** | −22.4 | −1.6e−4 | 4265 Hz |
| snare / tight | **+2.5** | −25.5 | −1.9e−4 | 2401 Hz |
| snare / lofi | **+3.1** | −21.7 | −8.3e−5 | 886 Hz |
| chat / metal | −14.1 | −47.4 | −5.3e−8 | 8783 Hz |
| chat / noise | −1.7 | −32.2 | −1.6e−7 | 10169 Hz |
| chat / tick | −3.1 | −36.0 | 1.3e−7 | 10225 Hz |
| ohat / metal | −10.2 | −38.6 | −4.8e−8 | 8770 Hz |
| ohat / noise | −0.5 | −23.6 | −4.4e−8 | 11375 Hz |
| clap / spread | −8.0 | −33.2 | −9.9e−6 | 1478 Hz |
| clap / tight | −11.3 | −39.1 | −1.1e−5 | 2878 Hz |
| clap / hall | −5.8 | −29.6 | 2.8e−5 | 1531 Hz |
| tom / analog | −0.1 | −20.5 | −4.2e−4 | 176 Hz |
| tom / synth | −0.6 | −22.6 | −2.4e−4 | 308 Hz |
| tom / wood | −0.4 | −20.8 | −3.7e−4 | 182 Hz |

### 확인된 문제

**A. 킥·스네어 소스가 0 dBFS를 넘습니다 (최대 +4.8 dB)**

킥은 4개 레이어(서브·바디·어택·클릭)가 각자 게인을 갖고 한 채널에 합산됩니다.
`Sub` 62 · `Punch` 60 기준으로 이론 최대 합이 약 2.49 (+7.9 dB),
위상차 덕에 실측은 +3.7 dB 입니다.

**B. 하이햇 엔진 간 음량 차가 12.4 dB**

`chat/metal` −14.1 vs `chat/noise` −1.7. 같은 자리에서 엔진만 바꿨는데
음량이 12 dB 넘게 달라지면 음색 비교가 불가능합니다.
클랩도 `tight` −11.3 vs `hall` −5.8 로 5.5 dB 차이가 납니다.

**C. 톰의 DC 오프셋이 상대적으로 큼**

−2.4e−4 ~ −4.2e−4. 판정 기준(1e−3) 이하라 당장 문제는 아니지만
악기 중 가장 크고, 사인 오실레이터를 0 위상에서 시작해 저역이 한쪽으로 치우칩니다.

### 문제가 아닌 것

- **클릭·팝 없음** — 모든 악기 첫 샘플 0.0, 엔벨로프가 0에서 시작해 0으로 끝남
- **꼬리 잘림 없음** — 마지막 200 샘플 RMS 가 전부 −∞ (완전 무음)
- **햇·클랩·톰은 헤드룸 안에 있음** — −0.1 ~ −14 dB

---

## 2. 실측 결과 — 마스터 체인 통과

하우스 밀집 패턴(킥 4온플로어 + 스네어 2·4 + 햇 8분, 124 BPM, 벨로시티 1.0),
`Master 74` · `Bus Trim −6 dB` · `Limit −2 dB` 기본값.

| 탭 지점 | 피크 dBFS | RMS dBFS | \|x\|>1 샘플 |
|---|---|---|---|
| 새추레이션 직전 (sumBus) | **+0.6** | −15.8 | 0.00% |
| 새추레이션 + 마스터게인 후 | −1.4 | −15.6 | 0.00% |
| **리미터 후 (최종 출력)** | **−0.5** | −14.6 | **0.00%** |

**결론: 지금 당장 소리가 깨지지는 않습니다.** 트랙 게인(snare 0.68 · chat 0.38)과
`Bus Trim −6 dB`, 글루 컴프레서가 소스의 과한 레벨을 흡수합니다.

**다만 헤드룸이 없습니다.** 새추레이션 직전이 이미 +0.6 dB 라
tanh 커브가 설계된 드라이브 이상으로 눌리고 있고, 사용자가
`Bus Trim` 을 0 으로 올리거나 · 여러 트랙을 동시에 강세로 치거나 ·
`Sub`/`Exciter` 를 올리면 바로 넘칩니다.

---

## 3. 코드 감사에서 나온 발견 — 측정으로 검증한 것만

에이전트 3곳(구현 감사 · 음색 설계 · 기타 합성)의 지적 중,
**제가 직접 측정해 확인한 것만** 싣습니다. 검증 하네스는 `tools/` 에 있습니다.

### 3-1. 🔴 `lowpass`/`highpass` 의 Q 는 **dB 단위**였습니다

`tools/verify-q.html` — 오프라인 렌더와 `getFrequencyResponse` 두 경로가 일치.

| Q 입력 | 선형이면 | dB이면 | **실측** |
|---|---|---|---|
| 5.0 | 5.000 | 1.778 | **1.778** |
| 18.1 | 18.100 | 8.035 | **8.035** |

`bandpass` 는 선형이 맞습니다(Q=5 → 실측 5.05).

**영향**: `acid` 의 `Q=5.0` 은 실제 선형 1.78 — **레조넌스가 사실상 없는 평범한 로우패스**였습니다.
303 의 스퀄치가 안 나던 근본 원인입니다. `reese` 의 `Q=1.6` 은 선형 1.20 으로 버터워스와 거의 같았습니다.

→ `Q_DB(선형값)` 헬퍼를 만들어 의도한 선형 Q 를 dB 로 변환하도록 고쳤습니다.

> 남은 문제: `Q=0.707` 로 버터워스를 의도한 곳이 **11군데** 있는데,
> 실제로는 선형 1.085(+0.72 dB 피크)입니다. 오차가 작고
> 357개 프리셋 음색이 전부 바뀌므로 이번엔 손대지 않았습니다.

### 3-2. 🔴 `mkCurve` 에 숨은 게인이 있습니다

`tools/verify-curve.html` — 이론값 `k/tanh(k)` 와 실측이 소수점 3자리까지 일치.

| k | 숨은 게인 |
|---|---|
| 1.1 | +2.8 dB |
| 1.8 | **+5.6 dB** |
| 2.8 | +9.0 dB |
| 4.0 | +12.0 dB |

**측정된 실제 피해**: 베이스 `Drive` 노브를 돌리면 커브가 바뀌는 지점에서

| Drive | 순 이득 | 계단 |
|---|---|---|
| 0.33 | +3.3 dB | |
| **0.34** | +6.7 dB | **+3.4 dB 점프** |
| 0.66 | +4.9 dB | |
| **0.67** | +7.9 dB | **+3.0 dB 점프** |

노브를 한 칸 움직이면 음량이 튀었습니다. → `CURVE_GAIN` 으로 보상하고 4단계로 세분했습니다.

마스터 셰이퍼도 `Drive=0` 에서 소신호↔대신호 **3.8 dB 압축**이 항상 걸려 있습니다
(이건 357개 프리셋 라우드니스가 전부 바뀌므로 보류).

### 3-3. 🟠 `Sub` 노브가 저역을 더하지 않고 콤필터를 만듭니다

`tools/tune-subpath.html` — 기본파만 Goertzel 로 추출해 측정.

| 50 Hz | 80 Hz | 110 Hz |
|---|---|---|
| **−5.9 dB** | −0.4 dB | **+5.1 dB** |

병렬 저역 새추레이션 경로(LP110 2단 = 위상 −180°)가 본선과 상쇄합니다.
**11 dB 스윙**이고, Sub 를 올릴수록 110 Hz 딥이 깊어집니다.

수정안 5가지를 측정 비교했으나 **어느 것도 깨끗하지 않았습니다**(스윙 7.5~20.5 dB).
병렬 새추레이션은 원본과 필터된 사본을 더하는 구조라 본질적으로 콤필터입니다.
제대로 고치려면 링크위츠-라일리 크로스오버로 대역을 나눠야 하는데,
그러면 `Sub=0` 에서도 소리가 달라져 **357개 프리셋 재검수**가 필요합니다. → 보류, 문서화만.

> **에이전트 제안을 반려한 사례**: "럼블 필터 뒤에서 분기하라"는 제안을 측정해보니
> 서브소닉(18.5 Hz)이 −4.6 dB → **+3.0 dB 로 7.6 dB 증가**했습니다.
> 현재 구조가 위상 상쇄로 *우연히* 서브소닉을 억제하고 있었습니다.

### 3-4. 🟠 클리핑 경고가 죽은 코드였습니다

`tools/verify-claims.html` — 마스터 체인 최대 도달 피크 측정.

| 조건 | 피크 |
|---|---|
| Master 100, Drive 0, 소스 8배 과입력 | −0.66 dBFS |
| Master 100, Drive 0, 정상 입력 | **−0.57 dBFS** |

`masterGain` 상한이 `vol/100*0.9 = 0.9` 이고 그 앞은 전부 이득 ≤ 1 이라
**출력이 −0.92 dBFS 를 넘을 수 없습니다.** 임계가 −0.3 dBFS 였으니 영원히 안 켜집니다.
→ 임계를 −1.2 dBFS 로 낮췄습니다.

### 3-5. 🔴 피드백 루프의 `DelayNode` 는 128샘플이 **더해집니다**

`tools/verify-delay.html` — 자기상관으로 실제 울린 음정을 역산.

| 목표 | 실측 |
|---|---|
| 82.41 Hz | **67.4 Hz** |
| 196.0 Hz | **128.0 Hz** |

요청 지연에 정확히 128샘플(1 렌더 퀀텀)이 추가됩니다.
보정하면(`1/f0 − 128/SR`) 상한이 **`SR/256` = 187.5 Hz** — 기타 G3(196 Hz)도 못 냅니다.

부수 확인:
- 사이클 **밖** 딜레이는 1샘플까지 정확 (피크 위치 콤에는 문제없음)
- "딜레이 없는 사이클은 무음"이라는 통설은 **사실이 아님** (피크 1.0 통과)

→ **기타는 라이브 루프가 불가능하고 오프라인 렌더링이 유일한 길입니다.**

---

## 4. 적용한 수정

| 항목 | 조치 | 검증 |
|---|---|---|
| `acid` 레조넌스 | `Q_DB(8.0)` = 18.06 dB (선형 8) + 스커트단 | 재생 확인 |
| `acid` 통과대역 | `hz*9` → `hz*54` (상한 5.2 kHz) | 재생 확인 |
| `reese` 통과대역 | `hz*7` → `hz*72` (상한 6.5 kHz) | 재생 확인 |
| `reese` 디튠 | cent 고정 → 목표 비트 3 Hz 역산 | 재생 확인 |
| `reese` 스테레오 | 3 톱니를 좌·중·우로 분리 | 재생 확인 |
| Drive 계단 | `CURVE_GAIN` 보상 + 4단계 | 재생 확인 |
| 글라이드 | 이전 음에서 출발하는 진짜 포르타멘토 | 재생 확인 |
| `fm` 인덱스 | 0.66 → 2.0, 모듈레이터 글라이드 누락 수정 | 재생 확인 |
| 클립 경고 | 임계 −0.3 → −1.2 dBFS | — |
| 오픈햇 참조 | 만료 시 자기 해제 | — |

357개 프리셋 전수 로드 · 베이스 5엔진 발음 · Drive 계단 지점 스윕 — 예외 0.

---

## 5. 아직 안 한 것

| 항목 | 이유 |
|---|---|
| `Q=0.707` 11곳 → 진짜 버터워스 | 357개 프리셋 음색이 전부 바뀜 |
| `mkCurve` 전면 정규화 | 라우드니스 재검수 필요 |
| `Sub` 경로 크로스오버화 | 위와 동일 |
| 건반·신스 트랙 신설 | 트랙 추가는 구조 변경 (10곳 연동) |
| 기타 트랙 신설 | 위와 동일 + KS 렌더러 신규 |
