# 이 저장소의 규칙

고치기 전에 읽으십시오. 규칙마다 **왜** 를 붙였습니다 — 이유를 모르면 지키다가
말게 됩니다. 원문과 사례는 링크에 있습니다.

여태 이 규칙들은 다섯 군데에 흩어져 있었습니다. 가장 강한 선언조차 장르 자료
878행 산문 속에 묻혀 있었습니다. 그래서 한곳에 모읍니다.

---

## 1. 자료 — 지어내지 않는다

**① 곡 이름을 지어내지 마십시오. 이 저장소에서 가장 나쁜 실패입니다.**
대표곡 표에 들어가는 곡은 **웹에서 실재를 확인하고, 출처가 그 장르로 분류한 것**만
입니다. 확인하지 못한 후보는 버리고, 버린 이유를 적습니다. 지어낸 곡이 한 줄이라도
들어가면 그 작업 전체가 무효입니다.
표를 만든 뒤에는 **다른 눈으로 URL 을 전부 다시 엽니다** — 실제로 그렇게 해서
「작곡자를 아티스트 칸에 적음」·「리다이렉트로 제목이 다름」 같은 것을 잡았습니다.

**② 선율·리프를 음 단위로 옮겨 적지 마십시오.** 저작권 문제이고, 이 도구가 필요로
하는 것도 아닙니다. 프리셋에 담는 것은 **성질**뿐입니다 — 템포 범위 · 드럼 골격 ·
베이스 역할 · 건반 종류 · 기타 음색.

**③ 아티스트·곡 이름을 생성 데이터에 넣지 마십시오.**
`genres/` 의 이름 목록은 **사람이 읽는 자료**입니다. 프로파일(`genres/profiles/`)과
AI 프롬프트에는 **파생된 서술 속성만** 씁니다. `evidenceBasis` 의 형식을 정규식으로
강제하는 이유가 이것입니다 — 곡 제목이 들어갈 문법적 자리를 없앤 것입니다.
→ [`genres/profiles/README.md`](genres/profiles/README.md) §2

**④ 「출처가 반대로 말하는 것」과 「출처가 아무 말도 없는 것」을 가르십시오.**
앞은 고칩니다. 뒤는 **값을 그대로 두고 «근거 없음» 만 표시**합니다.
근거 없는 숫자를 다른 근거 없는 숫자로 바꾸는 것은 발명을 발명으로 바꾸는 것입니다.
없는 차이를 숫자로 만들지도 마십시오 — 형제와 다를 근거가 없으면 형제와 같게 둡니다.

**⑤ 「이 문서에 X 가 없다」는 부재 주장은 반드시 직접 확인하십시오.**
조사를 남에게 맡겼다면 더욱 그렇습니다. 계열마다 과장이 나왔고, 그대로 믿었으면
**근거가 있는 값을 지울 뻔한 적이 여러 번**입니다.

---

## 2. 검사 — 임계값을 고쳐서 통과시키지 않는다

**⑥ 검사가 떨어지면 원인을 보십시오. 임계값이나 형식을 고쳐서 통과시키지 마십시오.**
회귀 시험은 **아직 안 고친 결함을 «예상된 실패»로 따로 셉니다**(`regression.mjs` 의
`KNOWN`). 결함이 고쳐지면 시험이 **실패로 돌아서** 목록을 갱신하라고 알립니다.

**⑦ 커밋 전에 검사를 돌리십시오.**

```
node tools/ci/all.mjs --static   # 브라우저 없이, 몇 초
node tools/ci/all.mjs            # 전부
```

**검사 목록은 `tools/ci/all.mjs` 한 곳에만 있습니다.** 여기에 한 줄 넣으면 README
와 `ci.yml` 이 따라옵니다. 목록을 두 곳에 적으면 반드시 갈라집니다 — 실제로 갈라져서
README 대로 다 돌려도 CI 에서만 떨어지는 상태였습니다.

**⑧ 귀로만 판단하지 마십시오.** 음색을 바꿨으면 `tools/` 의 하네스로 재고 공표된
악기 값과 대조합니다. 기타 기음이 27dB 죽어 있던 것도 측정으로만 드러났습니다.

---

## 3. 코드 — 클래식 스크립트의 제약

**⑨ `id` 와 같은 이름의 최상위 `function` 을 만들지 마십시오.**
`id` 가 붙은 요소가 같은 이름의 암묵 전역을 만듭니다. `duck()` 이 `#duck` 슬라이더와
부딪힌 적이 있습니다. 새 전역을 추가하기 전에 `check-globals.mjs` 를 돌리십시오 —
같은 이름을 두 파일이 `const` 로 선언하면 **그 뒤 스크립트가 전부 실행되지 않고
화면이 백지가 됩니다.**

**⑩ 외부 오리진을 다시 들이지 마십시오.** 이 페이지는 자기 자신 말고 아무 데도
접속하지 않습니다(샘플을 고를 때만 예외). Tone.js 와 폰트는 2026-08-15 에 저장소
안으로 들여왔습니다. → [`docs/perf/04-외부의존성.md`](docs/perf/04-외부의존성.md)

**⑪ 하네스에 앱 코드를 복사하지 마십시오.** `tools/_app-harness.js` 가 `src/` 를
직접 로드합니다. 예전에 복사해 쓰다가 **앱에서 나지 않는 소리를 재고 있었습니다.**

**⑫ `<script>` 순서가 곧 의존 순서입니다.** 파일을 끼웠으면
[`ARCHITECTURE.md`](ARCHITECTURE.md) 의 지도도 같이 고치십시오.

---

## 4. 문서와 데이터 — 사본은 기계가 맞춘다

**⑬ 원본을 고쳤으면 생성기를 다시 돌리십시오.**

| 원본 | 도구 | 생성물 |
|---|---|---|
| `genres/*.md` 의 레퍼런스·대표곡 표 · `billboard/*.md` · `genres/profiles/*.json` · `genres/forms/forms.json` | `node tools/build-refdata.mjs` | `src/data/references.js` · `billboard.js` · `profiles.js` · `songform.js` |
| `src/data/presets/*.js` | `node tools/sync-docs.mjs` | `genres/*.md` 의 «설정값» 표 · `patterns/*.md` 의 16칸 블록 |
| `genres/profiles/_batch-*.mjs` | `node genres/profiles/_gen.mjs <배치> <출력>` | `genres/profiles/*.json` |

**⑭ 프리셋 파일이 정본입니다.** `genres/` 의 설정값 표와 `patterns/` 의 패턴 블록은
**사본**입니다. 어긋나면 프리셋 파일이 맞고, `check-docs-sync.mjs` 가 잡습니다.

**⑮ 문서에 숫자를 적어 두지 마십시오.** 프리셋 수 · 엔진 수 · 형식 수 · 줄 수 ·
전역 선언 수 — 전부 낡습니다. 실제로 「곡 형식 13종」(실제 18) · 「최상위 선언
485개」(실제 522) · 「총 마디 32·48·…」(실제 64·80·…)로 낡아 있었고, 마지막 것은
**생성기의 템플릿에 박혀 다시 돌려도 틀린 채로 재생산**됐습니다.

수를 알아야 하면 문서가 아니라 기계에 물으십시오.

```
node tools/counts.mjs
```

⚠ **분모가 둘인 수가 있습니다.** 하위분기는 «이름» 으로 세는 것과 «(계열, 분기) 쌍»
으로 세는 것이 다릅니다(`뿌리` 는 A·B·D 가 함께 씁니다). 음색 배정은 이름 단위,
곡 형식 배정은 쌍 단위입니다 — **둘 다 맞는 수이니 한쪽을 다른 쪽으로 고치지
마십시오.** 문서의 수가 서로 다르면 먼저 «무엇을 세고 있는가» 를 보십시오.

**⑯ 프로파일 무리를 가르면 다섯 곳을 함께 옮기십시오** — ① 배치의 `SUB`
② `preset-index.js` 의 `PRESET_SUB` ③ 같은 파일의 음색 기본값 표 ④ `melody.js` 의
풀·리프 ⑤ `harmony.js` 의 `COMP_KIT`. ①②④ 는 검사가 잡지만 **③⑤ 는 조용히
기본값으로 떨어집니다.** 실제로 ⑤ 를 빠뜨려 배포까지 간 적이 있습니다.

**⑰ 시점 기록과 현행 명세를 섞지 마십시오.** `presets/*` · `ui-review.md` ·
`audio-review.md` · `docs/perf|ux|qa/*` · `consulting/*` 은 **그때의 기록**입니다.
현행 상태로 읽으면 이미 끝난 일을 또 하게 됩니다.

---

## 5. 이 도구가 무엇인지

빌드 단계가 없습니다. `pulse16-mk16.html` 을 열면 그대로 돕니다. 그래서 모든 데이터가
클래식 스크립트로 **얼려져** 있습니다 — 마크다운을 `fetch` 할 수 없기 때문입니다.
`package.json` 은 루트에 없고 `mcp/pulse-audit/` 한 곳에만 있습니다.

16스텝 4/4 한 마디가 격자입니다. 변박·셋잇단·12박은 **근사치만 가능**하고, 그런
장르는 그 사실을 문서에 적어 둡니다 — 표현할 수 없는 것을 표현한 척하지 않습니다.
